{
  "name": "test-demo",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "jest --watchAll --verbose --coverage",
    "e2e":"cypress open"
  },
  "devDependencies": {
    "cypress": "^9.3.1",
    "vite": "^2.7.2"
  }
}
